// Show / hide the "Stay Tuned" newsletter popup (Notify Me! button and close icon).
function toggle() {
  document.querySelector('.newsletter').classList.toggle('popUp');
}

// Open / close the floating social share menu.
function actionToggle() {
  document.querySelector('.action').classList.toggle('active');
}
